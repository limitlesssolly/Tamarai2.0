<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        Admin Product Management
    </title>
    <link rel="shortcut icon" type="image/jpg" href="/images/thebluelogo.png" />
    <link rel="stylesheet" href="/css/admin/admin-sellings.css">
</head>

<body>
    <%- include('../partials/adminNav.ejs') %>
        <div class="sellstoo" >
            <!-- <a onclick="nav()" class="editia">edits</a> -->
            <div class="viewing" id="viewing" style="display: block;">
                <div class="name">
                    <h4>
                        <%= Products.name %>
                    </h4>
                </div>
                <div class="brand">
                    <h4>Brand : <%= Products.brand %>
                    </h4>
                </div>
                <div class="seller">
                    <h4>Seller : <%= Products.seller%>
                    </h4>
                </div>
                <div class="price">
                    <h4>Price : <%= Products.price%>
                    </h4>
                </div>
                <div class="image">
                    <h4>
                        <%= Products.image%>
                    </h4>
                </div>
                <div class="count">
                    <h4>Count In Stock : <%= Products.count%>
                    </h4>
                </div>
                <div class="category">
                    <h4>Category : <%= Products.category%>
                    </h4>
                </div>
                <div class="description">
                    <h4>Description : <%= Products.description%>
                    </h4>
                </div>
                <div class="color">
                    <h4>Color : <%= Products.color%>
                    </h4>
                </div>
                <a href="/admin/dashboard/sellings/delete/<%=Products._id%>" class="delta">Delete</a>
            </div>
            <!-- <div class="updating" id="updating" style="display: none;">
                <form method="post" action="/admin/dashboard/sellings/view/update/<%=Products._id%>">
                    <div class="name"><input name="name" type="text"><%= Products.name %><div>
                    <div class="brand"><input name="brand" type="text"><%= Products.brand %></div>
                    <div class="seller"><input name="seller" type="text"><%= Products.seller%></div>
                    <div class="price"><input name="price" type="text"><%= Products.price%></div>
                    <div class="image"><input name="image" type="file"><%= Products.image%></div>
                    <div class="count"><input name="count" type="text" ><%= Products.count%></div>
                    <div class="description"><input name="description" type="text"><%= Products.description%></div>
                    <div class="category"><input name="category" type="text"><%= Products.category%></div>
                    <div class="color"><input name="color" type="color"><%= Products.color%></div>
                    <button type="submit" id="updatebutts"> Updatin </button>
                </form>
            </div> -->
        </div>
        <%- include('../partials/footer.ejs') %>
</body>
<script>
    function nav() {
        if (document.getElementById("updating").style.display == "block" ) {
            navClose();
        }
        else if (document.getElementById("updating").style.display == "none" ) {
            navOpen();
        }
    }
    function navOpen() {
        document.getElementById("updating").style.display = "block";
        
    }

    function navClose() {
        document.getElementById("updating").style.display = "none";
    }
</script>
</html>